machine:
  node:
    version: 6.11.0

dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm
  override:
    pwd: dist

deployment:
  pre:
    - npm run build
  staging:
    branch: master
    heroku:
      appname: appbee-dragon
