machine:
  node:
    version: 6.11.0

dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm

deployment:
  pre:
    - npm run build
  staging:
    branch: master
    heroku:
      pwd: ~/dist
      appname: appbee-dragon
