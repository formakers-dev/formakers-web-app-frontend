machine:
  node:
    version: 6.11.0

dependencies:
  pre:
    - rm -rf ./node_modules
  override:
    - npm install


deployment:
  development:
    branch: master
    heroku:
      appname: appbee-dragon
